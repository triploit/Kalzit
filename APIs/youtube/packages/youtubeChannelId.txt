$YoutubeChannelId = !fun ($idOrUrl ? String) {
	$regex = "http(s)?://(www\.)?youtube.com/channel/([a-zA-Z0-9_\-]+)".
	$matchRef ? Var = void.
	
	do: (idOrUrl switchFirst
		[{length: matchRef = regex match idOrUrl};{3 at ~matchRef}]
	);{idOrUrl}
}.
