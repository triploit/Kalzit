/* @kalzit.for Function

	This is a type that is only useful for marking parameters right now.
	It is used to indicate that something is used as a function (meaning that it might get called)
*/
$Function = $RepeatedFunction = {x}.