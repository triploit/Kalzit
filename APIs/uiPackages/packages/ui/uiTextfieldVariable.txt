`TODO: These functions seem to be broken`

$uiTextfieldVariableOnaction = ($ref ? Mutable ; $initialValue ? String) fun {ref = initialValue. {ref = x} uiTextfieldOnaction initialValue}.
$uiTextfieldVariableOnchange = ($ref ? Mutable ; $initialValue ? String) fun {
	ref = initialValue.
	$result = {ref = x} uiTextfieldOnchange initialValue.
	!listen ref {
		result uiSetText x
	}.
	result
}.
