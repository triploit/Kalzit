$uiCyclingSlides = ($pauseTime ? Int ; $_slideValues ? (List:Any)) fun {
	$slideIndexRef ? Var = void.
	$mySlides = slideIndexRef uiSlides _slideValues.
	$myIndexRef ? Var = 0.
	pauseTime repeatAsync {myIndexRef = (~myIndexRef + 1) mod length: slideValues. slideIndexRef = ~myIndexRef}.
	mySlides
}.
