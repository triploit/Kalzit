/* @kalzit.for pi
The constant number pi (about 3.14159)
*/
this.PI = Math.PI;

/* @kalzit.for e
The constant number e (about 2.71828)
*/
this.E = Math.E;

/* @kalzit.for abs
Returns the absolute value of a number.
Basically if the number is negative, the minus sign is removed.

Usage example:
```
$three = abs: 0 - 3.
```
*/
this.abs = function(num){return Math.abs(num)};