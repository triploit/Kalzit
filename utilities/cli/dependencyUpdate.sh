#Fetch linux updates...
sudo apt-get update
#... and install them
sudo apt-get upgrade

#Update NPM
sudo npm update -g

#Update youtube-dl (works only if installed without a package manager)
sudo youtube-dl --update